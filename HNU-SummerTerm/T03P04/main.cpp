// t03p04

#include <iostream>

using namespace std;

int main() {
    int n = 0, w = 0;
    cin >> n >> w;
    int *a = new int[n];
    for (int i = 0; i < n; ++i) {
        cin >> a[i];
    }
    // remain to be finished
    return 0;
}
