// h03p14

#include <iostream>
#include <algorithm>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    int a[4] = {0};
    for (int i = 0; i < 4; ++i) {
        cin >> a[i];
    }
    
    return 0;
}
