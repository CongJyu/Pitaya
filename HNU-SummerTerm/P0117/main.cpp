// p0117

#include <iostream>
#include <map>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    int n{};
    cin >> n;
    map<string, int> team;
    for (int i{}; i < n; ++i) {
        string name1, name2;
        int num1{}, num2{};
        
    }
    return 0;
}
